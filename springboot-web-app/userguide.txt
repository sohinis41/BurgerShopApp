Please clone the product to your local and open that product into workspace . You should be able to run it directly.

1.h2 database settings are default. So,
username=sa
password = 

you can also check h2 properties in the application.properties as commented out

2. Run BurgerWebApplication.java as spring boot application.